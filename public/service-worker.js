/* eslint-disable */
/*self.addEventListener("install", (event) => {
	event.waitUntil(
		caches.open("v1").then((cache) => {
			return cache.addAll([
				"./favicon.ico",
				"./index.html",
				"./logo192.png",
				"./logo512.png",
				"./manifest.json",
				"./robots.txt",
			]);
		})
	);
});*/
